#[derive(Clone, Copy)]
pub struct Rect {
    pub x: usize,
    pub y: usize,
    pub w: usize,
    pub h: usize,
}

#[derive(Clone, Copy)]
pub struct Row {
    pub x: usize,
    pub y: usize,
    pub w: usize,
}
